{
  "name": "self-hosted-electron-cdn",
  "version": "0.0.0",
  "description": "CDN that uses Cloudflare workers + R2",
  "homepage": "",
  "license": "UNLICENSED",
  "main": "src",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "deploy:prod": "npx wrangler deploy --env prod",
    "deploy:dev": "echo 'No dev deployment yet'",
    "start:prod": "npx wrangler dev --env prod --remote",
    "test": "npx jest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241011.0",
    "@types/jest": "^29.5.13",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5"
  },
  "dependencies": {
    "range-parser": "^1.2.1"
  }
}
